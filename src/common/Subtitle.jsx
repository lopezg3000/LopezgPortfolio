import React from 'react';

const Subtitle = ({ subtitle }) => {
    return (
        <div className='subtitle-wrapper'>
            <h2>{subtitle}</h2>
        </div>
    );
}

export default Subtitle;

